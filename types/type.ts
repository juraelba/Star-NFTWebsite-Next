export interface Color {
  name: string;
  hex: string;
  rgba: string;
  color: string;
  borderColor: string;
}

export interface HeroItem {
  name: string;
  icon: string;
  description: string;
  path: string;
  video: string;
}
